A graph has a total sum of all "degrees" of vertices to be atleast 2*(E) - E=No of edges

BITSET<n> can be useful when dealing with 0 and 1

Check out backtracking in graphs! Problem: "no_of_unique paths" using DFS!

Use map.count()>0 to check element's exitence instead if map.find() because 'find() returns iterator!

Inorder traversal of a binary SEARCH tree gives us a sorted array!

To display longlongint in c++: printf("%lld\n", x*y*z);

priority_queue <int, vector<int>, greater<int>> q; - To makr min-heap

printf("%.6f") for precision

import itertools
print(set(itertools.permutations([1,1,1,2]))) - UNIQUE PERMUTATIONS!		
